{
  "name": "elm-audio-tests",
  "main": "testRunner.js",
  "scripts": {
    "build": "ln -sf ../src; elm-make Audio.elm AsyncTests.elm --output=build/app.js",
    "test": "npm run build && electron main.js"
  },
  "dependencies": {
    "electron-prebuilt": "^1.0.2",
    "elm": "^0.17.0"
  }
}
